# 迭代复盘 · 2026-08

### 迭代 26 · CI/CD 管线 + 极简需求清单

- **背景**:用户确认按上轮讨论的管线最小方案落地,并要"按照需求出一份极简需求清单"。另:会话容器被回收,仓库在新路径重新克隆(记忆体系起效——读 STATUS/journal 即接上进度)。
- **做了**:① CI:`.github/workflows/ci.yml`——push/PR 触发 lint → `npm ci` → 装 Playwright Chromium → `npm test`(构建+72 E2E)→ 7 单测;并发去重;失败上传报告产物(playwright.config 的浏览器路径本就有云端回退逻辑,CI 零改动可跑)。② CD:`scripts/auto-deploy.sh` 拉取式部署(main 无新提交即静默退出;有则 npm ci → db:migrate → build → kickstart 重启;构建失败回滚上一版重建,旧服务保持在线);`macos-setup-server.sh` 新增 launchd 任务 `com.jizhangben.deploy`(StartInterval 600);部署.md「更新版本」章节改为全自动说明,排查表补"推了代码没上线"。③ `docs/极简需求清单.md`:一页版,P0/P1/分析守护/平台形态/数据运维分组,每行一条 + ✅/⬜/✖,并列"明确不做"(多用户/云托管/Key 进前端);README 索引置顶。
- **边界**:CI 在 GitHub 云端 runner 首次真实运行要等这次 push 后在 Actions 页确认;launchd 部署任务需在 MacBook 重跑一次 `macos-setup-server.sh` 才生效;数据库迁移不随回滚回退(增量设计 + 每日备份兜底,已写进文档)。
- **验证**:bash -n 两脚本、YAML 解析通过;本地完整执行 CI 同款命令序列(npm ci → lint → npm test → 单测)全绿,`npm run test:doc` 刷新测试报告 72/72。
- **结论**:push → CI 守门 → MacBook 十分钟内自动上线的闭环配置齐备;需求全景收敛为一页清单。
