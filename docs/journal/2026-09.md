# 迭代复盘 · 2026-09

### 迭代 27 · BytePlus 云发布管线

- **背景**:用户要求"自动化发布到 BytePlus 云的管线"。此前极简需求清单把"云服务托管"列为明确不做——用户此轮明确要云选项,视为决策更新:云为**可选形态**,默认仍自托管(记 ADR 0015,与 0011 并列而非取代)。
- **做了**:① `scripts/auto-deploy.sh` 泛化:重启动作按 `uname` 分支(macOS launchctl / Linux `sudo -n systemctl`),拉取→迁移→构建→失败回滚逻辑两形态共用一份,不复制脚本;② `scripts/byteplus-setup-server.sh`:Ubuntu ECS 一键配置——NodeSource 装 Node 20、`jizhangben.service` 常驻(崩溃拉起)、backup/deploy 两个 systemd timer(与 macOS launchd 三件套一一对应)、sudoers 白名单只放行 `systemctl restart jizhangben`;③ `.github/workflows/deploy-byteplus.yml`:`workflow_run` 挂在 CI 之后(绿才发布)+ 可手动触发,secrets(BP_HOST/BP_SSH_KEY/BP_USER/BP_PORT/BP_APP_DIR)未配置时输出提示并安静跳过——服务器端 10 分钟轮询兜底仍能自动上线,发布因此是"双通道幂等";④ docs/部署-BytePlus.md:选型对比表、ECS 购买要点(新加坡区与 ModelArk 同区、2C2G 防 OOM)、安全组只开 22、Tailscale 内网访问、只读 Deploy Key 拉码、secrets 配置、排查表、数据上云的诚实隐私说明;⑤ ADR 0015 + ADR 索引补全(此前欠账 0010–0014)+ README/部署.md/极简需求清单/STATUS/CHANGELOG 同步。
- **安全边界(延续铁律)**:应用端口 3000 不进安全组,访问走 Tailscale;ARK_API_KEY 只在服务器 `.env.local`;GitHub secrets 只存 SSH 发布密钥。
- **验证**:两脚本 `bash -n` 通过、workflow YAML 解析通过;push 后实测 CI 全绿、`Deploy BytePlus` 在未配 secrets 下按预期安静跳过(跳过路径即本轮可实测的部分);**ECS 实机开机与首次部署待用户操作确认**(文档已标注,惯例同 Termux)。应用源码零改动。
- **结论**:push → CI 守门 → MacBook(轮询)/BytePlus(SSH 秒级 + 轮询兜底)双形态发布就绪;用户按文档开一台 ECS 即可启用云端。
