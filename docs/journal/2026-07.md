# 迭代复盘 · 2026-07

按迭代记录**做了什么 / 遇到什么 / 结论**。新迭代往下追加。

---

### 迭代 1 · 需求与设计

- **做了**:确认平台(手机优先 Web)、OCR 来源、设计原则;产出需求文档(P0/P1/P2)、数据模型、页面结构;6 屏界面线框图。
- **遇到**:Canva 生成了 wireframe 候选,但沙盒网络策略拦截预览;改用自建 HTML 线框图。
- **结论**:文档先行,需求→数据模型→页面一次对齐,后续开发有据可依。

### 迭代 2 · 技术选型

- **做了**:对比三方案,定 Next.js 全栈 + SQLite + LLM 视觉 OCR(ADR 0001)。
- **结论**:开发效率与 OCR 质量优先;把决策与被否理由写进技术选型.md。

### 迭代 3 · 项目脚手架

- **做了**:create-next-app 初始化;Drizzle 7 表 schema、迁移、seed;页面骨架 + 底部导航;API 占位。
- **遇到**:CLAUDE.md 已存在导致脚手架初始化冲突(挪走再建)。
- **结论**:骨架能跑、库就绪、路由就位;build + 冒烟全绿。

### 迭代 4 · 记账表单 + 流水首页

- **做了**:记一笔(键盘/分类/账户/日期/备注/再记一笔)、编辑/删除、流水首页月度汇总与按日分组;transactions 增删改查 API。
- **遇到**:route 文件不能导出额外函数 → 校验抽到 lib/tx-validate。
- **结论**:记账核心闭环;Playwright 走查增/改/删/汇总一致。

### 迭代 5 · OCR 批量导入

- **做了**:lib/ocr 视觉识别、多图上传、待确认清单、去重、商户映射学习;识别与入账分离(ADR 0003/0004)。
- **结论**:产品核心卖点落地;mock 模式端到端验证识别→入账→去重全链路。

### 迭代 6 · 测试套件与管理入口

- **做了**:Playwright 19 例(TC-N/T/O/B)+ 独立测试库 + OCR mock + 手机视口;test/test:quick/test:list/test:report 入口;用例登记表(ADR 0008)。
- **遇到**:建库放 globalSetup 导致 `no such table`(webServer 先启动)→ 移到 webServer.command;getByText strict mode violation → 加 exact。
- **结论**:测试可零成本复现;并驱动发现了下一迭代的 bug。

### 迭代 7 · 批量修改 + 撤销

- **做了**:多选 → 批量改分类/账户/日期/删除,操作前快照 + restore 还原实现一次撤销(ADR 0006)。
- **遇到**:批量删空后空状态分支吞掉撤销 toast(TC-B3 发现并修复)。
- **结论**:P0 记账模块全部完成;测试 19/19。

### 迭代 8 · OCR 接入 BytePlus + 文档标准化

- **做了**:OCR 多 provider 适配(claude/byteplus,ADR 0005),默认 Seed-2.0-lite;provider 单测 7 例;README 重写;文档/记忆体系标准化(AGENTS/adr/STATUS/memory/journal/CHANGELOG)。
- **遇到**:BytePlus 文档站反爬(403),精确 model id 无法自动抓取 → 给默认值 + 引导用户从控制台取。
- **结论**:识别引擎可切换、可省钱、可合规;项目记忆资产成型,便于多 agent / 跨会话接力。

### 迭代 9 · P1 基金模块

- **做了**:更新 Canva 设计为实现版;基金模块全量落地——ocr.ts 重构抽出通用 `callVision` 并加基金识别;lib/fund 计算(总览汇总 + 跨期序列);基金 API(手动 upsert / 快照增删改 / OCR 识别 + 入账);前端总览、详情(表 + SVG 走势)、导入、手动记快照;新增 TC-F 7 例。
- **遇到**:① 手动 POST 的 body 字段是 `code`,而校验/入账期望 `fundCode`,未映射 → 校验报错(F3 用 fundCode 故先过,暴露不一致);API 内做映射修复。② 改代码后忘记 rebuild,Playwright 的 `npm run start` 跑旧 `.next`,测试仍红——rebuild 后全绿。
- **结论**:P1 完成;全量测试 26 E2E + 7 单测全绿。两个坑已沉淀到 lessons。

### 迭代 10 · 页面流程图 + P2 消费分析

- **做了**:生成页面流程图 SVG(可导入 Figma,存入 docs/);P2 首块消费分析——lib/analytics(月度趋势 / 分类结构 / 环比,纯函数基于现有流水,不新增表);/analysis 页面(环比卡 + 6 月趋势柱状 SVG + 分类结构条形);「我的」加入口;新增 TC-A 4 例。
- **遇到**:又一次 getByText strict mode("本月支出" 命中标签+标题)→ exact(已是已知教训,快速修)。
- **结论**:P2 起步;全量 30 E2E + 7 单测全绿。P2 剩余(预算/异常/订阅/月报)待后续 phase。

### 迭代 11 · P3 预算体系 + 超支提示

- **做了**:P3/P4 路线图落档;`budgets` 表(常设分类月度额度,ADR 0009——偏离路线图草案的"生效月"字段,理由:设一次月月生效的 UX 更适合个人);`/api/budgets` GET/PUT(>0 upsert、=0 删除);`/budget` 设置页;分析页「分类预算」进度区(≥80% 琥珀预警、超支红+标签);首页超支提醒卡;「我的」加入口;TC-BG 5 例。
- **遇到**:无新坑——strict-mode 选择器、rebuild 再测等已知教训均提前规避,TC-BG 首跑全绿。
- **结论**:P3 迭代 1 完成;全量 35 E2E + 7 单测全绿。经验库开始产生复利。

### 迭代 12 · P3 周期支出 + 到期提醒

- **做了**:`recurring` 表(名称/金额/分类/周期 monthly|yearly/下次支付日,ADR 0010);lib/recurring 纯函数(到期状态、月末安全顺延、近 3 个自然月同名同金额候选识别);API(增删改 + `/pay` 记一笔并顺延);`/recurring` 管理页(登记列表 + 到期标签 + 候选一键采纳);首页琥珀提醒卡(7 天内/过期,置于预算超支卡之前);「我的」加入口;TC-RC 5 例。
- **遇到**:一处旧坑复发被当场自纠——校验函数起初写在 route.ts 里(Next.js route 不允许导出额外函数),立刻抽到 lib/recurring-validate.ts。其余无新坑,TC-RC 首跑全绿。
- **结论**:P3 迭代 2 完成;全量 40 E2E + 7 单测全绿。「记一笔」写真实流水表、候选识别纯函数无新依赖,符合最小复杂度原则。

### 迭代 13 · P4 部署套件(macOS 家用服务器)

- **做了**:用户拍板部署形态「MacBook 放家里不关机 + Tailscale」(ADR 0011);`npm run setup` 一键环境搭建(装依赖+建库+seed+生成 .env.local,幂等);`scripts/backup.mjs`(better-sqlite3 在线备份 API,WAL 下运行中备份也一致;含 uploads 原图;轮转保留 30 份);`scripts/macos-setup-server.sh`(生成并加载 launchd plist:应用常驻 0.0.0.0:3000 + 每日 03:30 备份,幂等可重跑);docs/部署.md 全流程指南(防休眠、Tailscale、恢复、升级、排查)。
- **遇到**:无——路线图预判过「P4 部署不碰数据层,可并行插队」,实际确实零应用代码改动。launchd 在沙盒(Linux)无法实测,做了 bash -n 语法检查并把手动验证点写进部署文档;备份脚本实测两分支(生成/幂等)+ 轮转 + integrity_check ok。
- **结论**:部署路径打通,用户在 MacBook 上两条命令可上线;全量 40 E2E + 7 单测全绿(应用代码零改动,纯增量)。

### 迭代 14 · P4 访问保护(登录 + 注册)

- **做了**:users/sessions 表(迁移 0003);lib/auth(scrypt 密码、HttpOnly 会话、库存 sha256 token 可吊销);auth API(注册/登录/登出,重名 409、失败统一文案防枚举、ALLOW_REGISTER=0 关注册);/login /register 页;「我的」显示用户名+退出登录;**src/proxy.ts 中央守卫**——查随包文档确认 Next 16 proxy(原 middleware)默认 Node runtime 可直查 SQLite,一个文件保护全部 13 页面 + 14 API,新路由默认受保护,避免了 27 处逐一加守卫;测试基建:setup-db 写入固定 e2e 用户+长效会话,playwright storageState 注入 cookie,既有 40 例零改动照常过;TC-AU 6 例。
- **遇到**:最初写了逐路由守卫 lib/auth-guard.ts,查 Next 文档发现 proxy 默认 Node runtime 后改为中央拦截、删掉了守卫文件——「先查随包文档再动手」(铁律 8)再次奏效。
- **结论**:P4 访问保护完成;全量 46 E2E + 7 单测全绿。共享账本不做用户隔离(ADR 0012),公网化前置条件具备但仍推荐 Tailscale 纵深防御。

### 迭代 15 · 简化版 UI 移植到真实应用 + 测试报告体系

- **做了**:先在演示版完成简化设计并经用户确认方向,再移植真实应用——全局 15 个文件卡片投影→细描边扁平(一次 sed + 修复两处条件 border 冲突);首页 hero 卡(月份/结余/收支一卡)+ 单行提醒条(首项+计数);新增 TC-N3 验证新版首页,TC-RC2 断言随设计更新;`scripts/test-report.mjs` + `npm run test:doc`——从 Playwright results.json 生成 docs/测试报告.md(按功能模块汇总 + 明细,入库存档)。
- **遇到**:sed 全局替换撞上两处「条件 border」类名冲突(存疑项虚线边框),grep 复查发现并改为三元互斥;设计先在演示版收敛再动真实应用,移植零返工。
- **结论**:全量 47 E2E + 7 单测全绿;每个功能模块都有对应 TC 组和报告行,报告可随任意一次测试运行一键刷新。

### 迭代 16 · P3 净资产总览 + 月度报告

- **做了**:lib/report 纯函数(现金结余=账户初始+历史收支、netWorth、monthlyReport 含环比/分类 Top/最大单笔);`/networth` 构成条页;`/report` 月度报告页(月份切换);「我的」两个新入口,占位仅剩异常提醒;TC-NW 2 例 + TC-RP 3 例;测试报告刷新为 52/52。
- **遇到**:无新坑——复用 categoryBreakdown/budgetProgress/summarize,零新表(ADR 0013 记录口径:不做科目体系、报告不落库);networth 用例补了基金表清理(resetData 不含 funds)。
- **结论**:P3 迭代 3 完成;全量 52 E2E + 7 单测全绿。P3 仅剩异常检测。

### 迭代 17 · P3 收官:异常支出检测

- **做了**:lib/anomaly 纯函数(近 3 月基线;单笔 >3× 单笔均值且基线 ≥3 笔;分类 >2× 月均;≥¥50 下限防噪音,ADR 0014);按页面架构扩展位落地——首页 🔎 提醒条 + 分析页红色区块(标基线值);TC-AX 4 例。
- **遇到**:① 用例设计时发现大额单笔会同时触发分类异常(2 项)——确认这是预期信息互补而非 bug,用例按 2 项断言;② TC-F4 一次全量偶发失败,单跑与三次全量复跑均绿、失败 trace 已被覆盖,登记 STATUS 观察项待再现定位。
- **结论**:P3 全部完成;全量 59 E2E + 7 单测全绿,报告 59/59。

### 迭代 18 · 页面架构落地第 1 步(提醒源注册制)

- **做了**:抽 `components/ui/AlertStrip`(tone=amber|red);首页到期/超支/异常三条提醒重构为 alerts 数组 + map 渲染——新增提醒只需 push 一项。
- **结论**:纯重构零行为变化,全量 59 E2E + 7 单测全绿。剩余组件(PageHeader/Card/ProgressBar)留待下批。

### 迭代 19 · 按键位置重设计移植真实应用

- **做了**:延续「演示版先收敛设计→再移植」模式——BottomNav 改 5 槽对称(＋居中,分析升一级 tab,analysis 页面并入 (tabs) 路由组、URL 不变);基金页操作下沉为总览卡下等宽双按钮;周期支出「新增」改全宽虚线按钮;首页导入按钮降 ghost;TC-N2 增加分析 tab 断言、TC-RC1 适配新按钮文案。
- **遇到**:TC-N2 首跑失败——按演示版文案写了「近 6 月」而真实应用是「近 6 个月」,修正断言即绿(教训:跨版本断言以目标版本文案为准)。
- **结论**:全量 59 E2E + 7 单测全绿;两版设计完全一致。下一步 P4。

---

## 下一步

P4(PWA 加主屏 / AI 问答);演示版待同步净资产/月报/异常(见 STATUS)。见 [路线图](../路线图.md) / [STATUS.md](../STATUS.md)。
