# 测试用例登记表

> 端到端测试基于 Playwright,手机视口(390×844)+ 独立测试库(`data/test.db`)+ OCR mock,不触碰开发数据。
> 用例编号与 `tests/*.spec.ts` 中的测试标题一一对应,新增用例时先在此登记再写代码。

## 入口(如何跑 / 如何看)

| 命令 | 作用 |
|------|------|
| `npm test` | 完整跑:先构建再执行全部用例 |
| `npm run test:quick` | 快速跑:复用已有构建(改了测试没改代码时用) |
| `npx playwright test tests/ocr.spec.ts` | 只跑某个套件 |
| `npx playwright test -g "TC-T5"` | 只跑某个用例(按编号过滤) |
| `npm run test:list` | 列出全部用例(不执行) |
| `npm run test:report` | 打开上次运行的 HTML 报告(含每个用例的页面截图、失败 trace) |

报告产物:`playwright-report/`(HTML + `results.json`,已 gitignore)。每个用例自动截图,失败时保留 trace 可回放。

## TC-N 页面与导航(`tests/navigation.spec.ts`)

| 编号 | 用例 | 验证点 |
|------|------|--------|
| TC-N1 | 五个页面均可访问 | 流水/记一笔/导入/基金/我的 都渲染核心元素 |
| TC-N2 | 底部导航切换 | 点击跳转正确路由 |

## TC-T 记账核心(`tests/transactions.spec.ts`)

| 编号 | 用例 | 验证点 |
|------|------|--------|
| TC-T1 | 手动记一笔支出 | 键盘输入 32.50 → 首页显示、库中 1 条 |
| TC-T2 | 收支结余计算 | 支出 35 + 收入 12000 → 结余 +11,965.00 |
| TC-T3 | 表单校验 | 空金额/未选分类被拦截并提示,不写库 |
| TC-T4 | 再记一笔 | 连续录入不离开页面 |
| TC-T5 | 编辑条目 | 点流水进编辑页,改金额生效 |
| TC-T6 | 删除条目 | 确认弹窗 → 删除后首页与库中均消失 |
| TC-T7 | 月份切换 | 无账目月份显示空状态 |

## TC-O OCR 导入(`tests/ocr.spec.ts`)

| 编号 | 用例 | 验证点 |
|------|------|--------|
| TC-O1 | 识别生成待确认清单 | 4 笔,存疑项标黄、默认不勾,未入账前不写库 |
| TC-O2 | 勾选入账 | 写入 3 条 OCR 流水、汇总 ¥88.40、📎 标记、学习商户映射 |
| TC-O3 | 重复检测 | 同批再导入全部标"疑似重复"且默认 0 勾选 |
| TC-O4 | 空状态 | 未选图时无识别按钮,显示上传引导 |
| TC-O5 | 全选 | 「已选」按钮一键勾上含存疑的全部记录 |

## 待补充(功能实现后)

- TC-B 批量修改组:多选流水 → 批量改分类/删除(功能未实现)
- TC-F 基金组:持仓截图识别、同名基金跨期汇总、同日重复快照提示(功能未实现)
- 真实 API 冒烟用例:不用 mock,传真实截图验证识别质量(需 ANTHROPIC_API_KEY,手动触发)
